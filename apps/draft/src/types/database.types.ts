/**
 * Re-export of the canonical generated database types, which now live in the
 * shared workspace package @axoloto/supabase so both the draft and team apps
 * share one source of truth. Regenerate with the Supabase CLI into
 * packages/supabase/src/database.types.ts.
 */
export * from '@axoloto/supabase/types'
